<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      component_business_basic —— 业务基础页面模块。

      定位：登录页、错误页 / 空态页、权限引导页这类**需要被多个业务复用**的公共页面。
      目前有：登录页。

      这里的组件会被合并进 app 的清单，所以业务侧不用再声明一遍。
    -->
    <application>

        <activity
            android:name="com.tscaffold.business.basic.login.view.LoginActivity"
            android:exported="false"
            android:theme="@style/business_basic_theme"
            android:windowSoftInputMode="adjustResize" />

    </application>

</manifest>
